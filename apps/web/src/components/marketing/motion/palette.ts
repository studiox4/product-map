/**
 * Shared color tokens for the marketing motion SVGs (hero graphic + story SVGs).
 * Matches the canonical "Stagger" brand mark (BrandMark.tsx / docs/brand): two
 * indigo streams plus a lighter playhead/today line. Centralized so the gantt
 * graphics stay in lockstep if the brand indigo ever shifts.
 */
export const STAGGER_INK = '#4338CA'; // deep stream (rows 1 & 3)
export const STAGGER_LIT = '#6D63F0'; // lit stream (rows 2 & 4)
export const PLAYHEAD = '#818CF8'; // "today" playhead / sweep line
