DATABASE_URL=postgres://localhost:5432/productmap
# AI drafting via Amazon Bedrock — setting any of AWS_REGION / AWS_PROFILE /
# AWS_ACCESS_KEY_ID enables it; the standard AWS credential chain does the rest.
AWS_REGION=us-east-1
AWS_PROFILE=
BEDROCK_MODEL_ID=
